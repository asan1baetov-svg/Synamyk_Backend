package synamyk.enums;

/** Deep-link hint sent in the FCM data payload so the client knows which screen to open. */
public enum PushDataType {
    NONE,
    TEST,
    SUB_TEST,
    GAME,
    BROADCAST
}
